mutation CreateProductMedia($id: ID!, $media: [CreateMediaInput!]!) {
  productCreateMedia(
    productId: $id
    media: $media
  ) {
    media {
      alt
      mediaErrors {
        message
      }
      ... on MediaImage {
        originalSource {
          url
        }
      }
    }
  }
}
