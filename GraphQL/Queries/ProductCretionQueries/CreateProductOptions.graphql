mutation CreateProductOptions($id: ID! , $productOptions : [OptionCreateInput!]!) {
    productOptionsCreate(
        productId: $id
        options: $productOptions
    ) {
        product {
            bodyHtml
            options(first: 5) {
                id
                name
                values
            }
        }
    }
}

